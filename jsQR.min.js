
// MOCK jsQR minimal stub per evitare errori lato client (da sostituire con jsQR reale da CDN)
function jsQR(data, width, height) {
  console.warn("jsQR non disponibile in questa versione offline. Caricare jsQR reale da CDN.");
  return null;
}
